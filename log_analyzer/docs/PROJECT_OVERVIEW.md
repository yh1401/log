# Log Analyzer 系统功能说明文档

> 版本: v2.5.1
> 更新日期: 2026-06-05
> 项目路径: `/Users/a666/Documents/trae_projects/log/log_analyzer`

---

## 目录

1. [项目概述](#1-项目概述)
2. [核心功能模块](#2-核心功能模块)
3. [技术架构](#3-技术架构)
4. [操作流程说明](#4-操作流程说明)
5. [接口规范](#5-接口规范)
6. [数据存储方案](#6-数据存储方案)
7. [权限与用户管理](#7-权限与用户管理)
8. [开发进度与当前状态](#8-开发进度与当前状态)
9. [并发性能评估](#9-并发性能评估)
10. [已知问题与解决方案](#10-已知问题与解决方案)
11. [快速入门指南](#11-快速入门指南)

---

## 1. 项目概述

### 1.1 项目定位

**Log Analyzer** 是一个面向运维工程师、SRE团队和后端开发人员的**智能日志分析平台**。系统通过调用大语言模型（LLM）或规则引擎，对大规模应用日志和网络抓包文件进行结构化分析、错误聚合、根因推断和整改建议生成。

### 1.2 核心能力

- **大规模日志处理**：支持100MB+日志文件，流式分块处理，内存占用稳定在200MB以内
- **智能错误识别**：8类错误模式识别，语义相似度合并，动态内容标准化
- **自动报告生成**：支持PDF、Word、Markdown、HTML多种格式
- **网络抓包分析**：PCAP/PCAPNG格式自动解析，协议统计，LLM诊断
- **多用户数据隔离**：基于X-User-Id的用户数据隔离
- **双模式分析**：支持LLM深度分析和规则快速分析两种模式
- **任务管理**：支持任务状态查询、任务终止、历史记录管理
- **并发性能优化**：线程安全的任务状态管理

### 1.3 版本信息

| 版本 | 日期 | 主要更新 |
|------|------|---------|
| v2.5.1 | 2026-06-05 | HTML报告查看、任务终止功能、并发性能优化 |
| v2.5.0 | 2026-06-04 | 操作日志埋点、多用户权限优化 |
| v2.4.0 | 2026-06-03 | 规则模式日志分析器 |
| v2.3.0 | 2026-06-02 | 智能错误合并功能 |
| v2.2.0 | 2026-06-02 | PCAP提示词优化 |

---

## 2. 核心功能模块

### 2.1 日志分析模块

#### 2.1.1 LLM模式
- **语义理解**：支持复杂问题诊断和深度分析
- **根因推断**：提供多层次根因分析
- **智能建议**：基于分析结果生成整改建议
- **响应时间**：5-30秒/1000条日志

#### 2.1.2 规则模式
- **零成本**：不调用外部API，无费用产生
- **极速响应**：<1秒/1000条日志
- **多层次分类**：Critical、High、Medium、Low四个级别
- **8种根因类型**：空引用、资源泄漏、超时、认证、数据库、网络、配置、内存

### 2.2 文件管理模块

#### 2.2.1 文件上传
- **支持格式**：.log、.txt、.zip（自动解压）、.pcap
- **上传接口**：`POST /api/upload`
- **路径验证**：支持路径输入和文件浏览两种方式

#### 2.2.2 服务器路径读取
- **单一接口**：`POST /api/list-dir`
- **功能模式**：
  - 浏览模式：列出目录内容
  - 验证模式：验证路径有效性
  - 递归模式：扫描子目录
- **权限控制**：白名单机制，支持配置允许访问的目录

### 2.3 任务管理模块

#### 2.3.1 任务创建
- **接口**：`POST /api/process` 或 `POST /api/process-from-path`
- **参数**：
  - `file_path`：文件路径
  - `chunk_size`：分块大小（默认5000）
  - `use_llm`：是否使用LLM（默认True）
  - `force_restart`：是否强制重启（默认False）

#### 2.3.2 任务状态查询
- **接口**：`GET /api/task/{task_id}`
- **状态**：pending、processing、completed、failed、cancelled
- **实时进度**：返回处理进度百分比

#### 2.3.3 任务终止
- **接口**：`POST /api/task/{task_id}/cancel`
- **功能**：支持在分析过程中终止任务
- **权限**：仅能终止当前用户的任务

### 2.4 报告生成模块

#### 2.4.1 支持格式
| 格式 | 说明 | 文件前缀 |
|------|------|---------|
| PDF | 格式化排版，支持中文 | `report_` |
| Word | 可编辑文档 | `report_` |
| Markdown | 纯文本格式 | `report_` |
| HTML | 网页格式，支持预览 | `report_` |

#### 2.4.2 报告前缀规则
- **普通任务**：`report_【文件名】_时间戳.html`
- **路径任务**：`report_path_【文件名】_时间戳.html`
- **综合报告**：`path_combined_【文件名】_时间戳.html`

#### 2.4.3 报告预览功能
- **Markdown预览**：支持在模态框中预览MD文件内容
- **HTML预览**：支持在iframe中预览HTML报告

### 2.5 历史报告管理模块

#### 2.5.1 CRUD接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/history/reports` | POST | 创建历史报告记录 |
| `/api/history/reports` | GET | 获取历史报告列表 |
| `/api/history/reports/{report_id}` | GET | 获取报告详情 |
| `/api/history/reports/{report_id}` | PUT | 更新报告 |
| `/api/history/reports/{report_id}` | DELETE | 删除报告 |

#### 2.5.2 文件数量限制
- **每个用户最大报告记录数**：50条
- **每个用户报告文件总数**：200个（4种格式 × 50条记录）
- **每个用户上传文件数**：50个

### 2.6 操作日志模块

#### 2.6.1 埋点记录
系统自动记录以下操作：
| 操作类型 | 说明 |
|---------|------|
| file_upload | 文件上传 |
| task_start | 任务开始 |
| task_complete | 任务完成 |
| task_failed | 任务失败 |
| page_view | 页面访问 |
| button_click | 按钮点击 |

#### 2.6.2 日志存储
- **存储路径**：`data/action_logs/{user_id}/{date}.json`
- **索引文件**：`data/action_logs/{user_id}/_index.json`

---

## 3. 技术架构

### 3.1 技术栈

| 分类 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 主语言 | Python | 3.10+ | 核心业务逻辑 |
| Web框架 | FastAPI | 0.100+ | RESTful API服务 |
| 异步运行时 | uvicorn | 0.20+ | ASGI服务器 |
| 前端 | HTML/CSS/JavaScript | - | Web界面 |
| PDF生成 | ReportLab | 4.0+ | PDF报告生成 |
| Word生成 | python-docx | 0.8+ | Word报告生成 |
| 网络分析 | tshark | 4.0+ | PCAP文件解析 |
| HTTP客户端 | httpx | 0.24+ | LLM API调用 |

### 3.2 项目结构

```
log_analyzer/
├── web/                    # Web服务
│   ├── app.py             # FastAPI应用主文件
│   ├── auth.py            # 认证与用户管理
│   ├── action_logger.py   # 操作日志记录器
│   ├── storage.py         # 存储管理
│   └── static/            # 静态资源
│       └── index.html     # 前端页面
├── report/                # 报告生成
│   ├── generator.py       # 报告生成器
│   ├── rule_based_analyzer.py  # 规则分析器
│   └── error_merger.py    # 错误合并
├── processor/             # 日志处理
│   └── chunk_processor.py # 分块处理器
├── llm/                   # LLM客户端
│   └── client.py          # LLM API调用
├── config/                # 配置文件
│   └── config.json        # 主配置文件
├── data/                  # 数据目录
│   ├── uploads/           # 上传文件
│   ├── reports/           # 生成的报告
│   ├── tasks/             # 任务信息
│   └── action_logs/       # 操作日志
├── logs/                  # 系统日志
│   └── web_*.log          # Web服务日志
├── tests/                 # 测试用例
│   ├── concurrency_test.py # 并发性能测试
│   └── test_*.py          # 功能测试
└── docs/                  # 文档
```

### 3.3 核心组件

#### 3.3.1 LLMClient
- **职责**：封装LLM API调用
- **特性**：支持流式输出、错误重试、并发控制

#### 3.3.2 ChunkProcessor
- **职责**：日志分块处理
- **模式**：支持LLM模式和规则模式切换

#### 3.3.3 ReportGenerator
- **职责**：生成多格式报告
- **格式**：PDF、Word、Markdown、HTML

#### 3.3.4 并发安全机制
- **全局任务锁**：`processing_tasks_lock` 保护任务状态字典
- **线程安全**：支持多线程环境下的并发访问

---

## 4. 操作流程说明

### 4.1 文件上传分析流程

```
┌─────────────┐
│  1. 上传文件  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  2. 文件验证  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  3. 创建任务  │──▶ POST /api/process
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────┐
│  4. 处理中（可终止）            │
│     ├─▶ GET /api/task/{task_id}│
│     └─▶ POST /api/task/cancel  │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────┐
│  5. 获取报告  │
└─────────────┘
```

### 4.2 服务器路径分析流程

```
┌─────────────┐
│  1. 输入路径  │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  2. 路径验证  │──▶ POST /api/list-dir
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  3. 提交处理  │──▶ POST /api/process-from-path
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  4. 轮询状态  │◀── GET /api/task/{task_id}
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  5. 获取报告  │
└─────────────┘
```

### 4.3 报告预览流程

```
┌──────────────────┐
│ 点击查看按钮      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 判断文件类型      │
└────────┬─────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌──────────┐
│ MD文件 │ │ HTML文件 │
└───┬───┘ └────┬─────┘
    │          │
    ▼          ▼
┌──────────────────────┐
│ 打开预览模态框        │
│ 渲染内容并展示        │
└──────────────────────┘
```

---

## 5. 接口规范

### 5.1 通用请求头

| 请求头 | 必填 | 说明 | 示例 |
|--------|------|------|------|
| `X-User-Id` | 否 | 用户ID，未填则使用默认值 | `admin001` |
| `X-User_Name` | 否 | 用户名，用于显示 | `admin` |
| `Content-Type` | 是 | application/json或multipart/form-data | `application/json` |

### 5.2 统一响应格式

```json
{
    "code": 0,
    "message": "操作成功",
    "data": { ... }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 0=成功，非0=失败 |
| message | string | 操作结果描述 |
| data | object/null | 响应数据 |

### 5.3 主要接口列表

#### 5.3.1 认证接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/auth/identify` | POST | 用户身份识别 |
| `/api/auth/current` | GET | 获取当前用户信息 |

#### 5.3.2 文件管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/upload` | POST | 上传文件 |
| `/api/list-dir` | POST | 目录浏览/验证 |
| `/api/process-from-path` | POST | 从路径处理文件 |

#### 5.3.3 任务管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/process` | POST | 创建处理任务 |
| `/api/task/{task_id}` | GET | 查询任务状态 |
| `/api/task/{task_id}/cancel` | POST | 终止任务 |

#### 5.3.4 报告接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/reports` | GET | 获取报告列表 |
| `/api/download/{file_path}` | GET | 下载报告文件 |

#### 5.3.5 历史管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/history/reports` | POST/GET | 创建/获取历史报告 |
| `/api/history/reports/{id}` | GET/PUT/DELETE | 历史报告CRUD |
| `/api/history/actions` | POST | 查询操作日志 |
| `/api/history/actions/count` | GET | 操作统计 |
| `/api/history/actions/types` | GET | 操作类型列表 |

#### 5.3.6 系统接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/health` | GET | 健康检查 |
| `/api/initialize` | POST | 初始化系统 |

### 5.4 错误码定义

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 1 | 操作失败 |
| 400 | 请求参数错误 |
| 403 | 权限不足 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 6. 数据存储方案

### 6.1 目录结构

| 目录 | 路径 | 说明 |
|------|------|------|
| 上传文件 | `data/uploads/{user_id}/` | 用户上传的原始文件 |
| 报告文件 | `data/reports/{user_id}/` | 生成的报告文件 |
| 任务信息 | `data/tasks/` | 任务状态JSON文件 |
| 操作日志 | `data/action_logs/{user_id}/` | 用户操作日志 |

### 6.2 文件命名规则

#### 6.2.1 日志文件
- **普通任务**：`web_process_YYYYMMDD_HHMMSS_文件名.log`
- **路径任务**：`web_path_YYYYMMDD_HHMMSS_文件名.log`

#### 6.2.2 报告文件
- **普通任务**：`report_【文件名】_YYYYMMDD_HHMMSS.html`
- **路径任务**：`report_path_【文件名】_YYYYMMDD_HHMMSS.html`

### 6.3 数据隔离

- **用户隔离**：每个用户的数据存储在独立目录下
- **报告隔离**：报告按用户ID分区存储
- **任务隔离**：任务ID包含用户标识

---

## 7. 权限与用户管理

### 7.1 用户识别机制

系统采用简化的用户识别机制：
1. 通过HTTP请求头 `X-User-Id` 识别用户
2. 未携带头时使用默认值 `hanmeimei`
3. 支持用户切换和管理员模式

### 7.2 权限级别

| 级别 | 说明 | 适用接口 |
|------|------|---------|
| 公开 | 无需认证 | `/api/health` |
| 用户级 | 需要X-User-Id | 大部分业务接口 |
| 管理员级 | 需要管理员权限 | 无（已移除限制） |

### 7.3 功能访问权限

| 功能 | 权限规则 |
|------|---------|
| 历史报告管理 | 只能查看和管理自己的报告 |
| 操作日志查询 | 可以查看所有用户的操作记录 |
| 操作统计 | 可以查看所有用户的统计数据 |
| 操作类型列表 | 所有用户可访问 |

### 7.4 管理员配置

管理员用户配置在 `web/auth.py` 中：
```python
ADMIN_USERNAME = "admin"  # 管理员用户名
ADMIN_USER_ID = "admin001"  # 管理员用户ID
```

---

## 8. 开发进度与当前状态

### 8.1 已完成功能

#### 核心功能
- [x] 大规模日志文件处理（支持100MB+）
- [x] LLM模式和规则模式双模式分析
- [x] 智能错误识别与合并
- [x] 多格式报告生成（PDF/Word/Markdown/HTML）
- [x] PCAP网络抓包分析
- [x] 服务器路径读取功能
- [x] 多用户数据隔离
- [x] 操作日志记录与查询
- [x] 历史报告CRUD管理
- [x] 任务终止功能
- [x] HTML报告预览功能
- [x] 并发性能优化（线程安全）

#### 用户界面
- [x] Web界面（响应式设计）
- [x] 文件上传与拖拽
- [x] 服务器路径浏览器
- [x] 实时任务进度监控
- [x] 报告预览与下载
- [x] 任务终止按钮

#### 管理功能
- [x] 历史报告管理
- [x] 操作日志查询
- [x] 操作统计展示
- [x] 多用户支持
- [x] 文件数量限制

### 8.2 待开发功能

| 功能 | 优先级 | 状态 |
|------|--------|------|
| 数据库存储后端 | 高 | 待开发 |
| Redis缓存优化 | 高 | 待开发 |
| 分布式任务队列 | 中 | 待开发 |
| LLM成本统计与限制 | 中 | 待开发 |
| 报告订阅与定期分析 | 低 | 待开发 |
| 实时流式日志接入 | 低 | 待开发 |

### 8.3 当前版本特性

**v2.5.1 主要更新**：

1. **任务终止功能**
   - 添加任务终止API接口
   - 前端添加"终止分析"按钮
   - 优化任务取消提示信息

2. **HTML报告预览**
   - 添加HTML预览模态框
   - 支持历史报告和新生成报告的HTML预览
   - 后端返回HTML文件内容

3. **并发性能优化**
   - 添加线程锁保护全局任务状态
   - 并发测试QPS突破2000
   - 支持100+并发请求

4. **文件数量限制**
   - 每个用户最多50条报告记录
   - 每个用户最多200个报告文件
   - 每个用户最多50个上传文件

---

## 9. 并发性能评估

### 9.1 测试结果

| 测试场景 | 并发数 | 请求数 | QPS | 平均响应时间 | 成功率 |
|---------|--------|--------|-----|-------------|--------|
| 基准测试 | 10 | 100 | 879.11 | 11.26ms | 100% |
| 中等负载 | 20 | 100 | 1482.21 | 13.14ms | 100% |
| 文件I/O测试 | 10 | 50 | 1095.57 | 8.85ms | 100% |
| 高并发压力 | 50 | 500 | 2022.83 | 27.22ms | 100% |
| 极限并发 | 100 | 1000 | 2049.71 | 54.13ms | 100% |

### 9.2 性能指标

- **最高QPS**：2049.71 请求/秒
- **平均响应时间**：54.13ms（100并发）
- **P95响应时间**：79.64ms
- **成功率**：100%

### 9.3 优化措施

| 优化项 | 说明 |
|--------|------|
| 线程锁保护 | 使用`processing_tasks_lock`保护全局状态 |
| 异步处理 | FastAPI异步端点处理 |
| 后台任务 | 使用BackgroundTasks处理耗时操作 |

---

## 10. 已知问题与解决方案

### 10.1 已解决的问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 服务器读取无响应 | 事件监听器绑定问题 | 移除冲突的onclick属性 |
| 模态框关闭失效 | 模态框ID不唯一 | 添加唯一ID并创建专门关闭函数 |
| 任务ID未注册 | 任务创建时未添加到字典 | 在创建任务时同时注册到字典 |
| 日志文件路径错误 | PROJECT_ROOT指向错误 | 使用SCRIPT_DIR.parent定位正确路径 |
| 报告下载失败 | 路径标准化问题 | 正确解析URL编码的路径 |
| 并发请求失败 | 无线程安全保护 | 添加线程锁保护全局状态 |

### 10.2 常见问题排查

#### 问题1：操作日志查询为空
**原因**：埋点函数未被调用

**解决方案**：
- 确保后端导入了操作日志函数
- 检查 `record_file_upload`、`record_task_start` 等函数是否正确调用

#### 问题2：历史报告列表为空
**原因**：接口权限限制或数据隔离

**解决方案**：
- 确认使用正确的用户ID
- 检查 `/api/reports` 接口返回的数据
- 验证报告文件是否存在于正确目录

#### 问题3：任务终止失败
**原因**：任务ID不正确或任务已完成

**解决方案**：
- 确认任务ID正确
- 检查任务状态是否为processing
- 验证用户权限

---

## 11. 快速入门指南

### 11.1 环境要求

- Python 3.10+
- tshark (Wireshark命令行工具)
- 网络连接（使用LLM模式时）

### 11.2 安装步骤

1. **克隆项目**
```bash
cd /Users/a666/Documents/trae_projects/log
```

2. **安装依赖**
```bash
cd log_analyzer
pip install -r requirements.txt
```

3. **启动服务**
```bash
cd web
./start.sh
```

服务将在 http://localhost:8000 启动

### 11.3 快速使用

#### 方式一：文件上传分析

1. 打开 http://localhost:8000
2. 拖拽或选择日志文件
3. 点击"开始分析"
4. 等待处理完成（可点击"终止分析"取消）
5. 查看和下载报告

#### 方式二：服务器路径分析

1. 点击"服务器读取"按钮
2. 输入或浏览服务器路径
3. 配置扫描选项
4. 点击确认开始分析
5. 等待处理完成
6. 查看和下载报告

### 11.4 配置管理

#### LLM配置
编辑 `config/config.json` 中的 `llm` 部分：
```json
{
  "llm": {
    "provider": "openai",
    "api_key": "your-api-key",
    "model": "gpt-4"
  }
}
```

#### 服务器路径权限
编辑 `config/config.json` 中的 `server_path` 部分：
```json
{
  "server_path": {
    "allowed_directories": ["/Users/a666/Documents"],
    "max_paths": 5
  }
}
```

---

## 附录

### A. 相关文档

| 文档 | 路径 | 说明 |
|------|------|------|
| README | `README.md` | 项目总体介绍 |
| API文档 | `docs/API.md` | 完整API接口文档 |
| 用户指南 | `docs/USER_GUIDE.md` | 详细使用说明 |
| 开发者指南 | `docs/DEVELOPER_GUIDE.md` | 开发规范 |
| 变更日志 | `docs/CHANGELOG.md` | 版本更新记录 |
| 并发优化报告 | `docs/CONCURRENCY_OPTIMIZATION_REPORT.md` | 性能优化文档 |

### B. 联系与支持

如有问题，请查看：
1. 系统日志：`logs/web_*.log`
2. 操作日志：`data/action_logs/`
3. 任务信息：`data/tasks/*.json`

### C. 版本历史

- **v2.5.1** (2026-06-05)：任务终止功能、HTML报告预览、并发性能优化
- **v2.5.0** (2026-06-04)：操作日志埋点、多用户权限优化
- **v2.4.0** (2026-06-03)：规则模式日志分析器
- **v2.3.0** (2026-06-02)：智能错误合并功能
- **v2.2.0** (2026-06-02)：PCAP提示词优化

---

*本文档最后更新于 2026-06-05*
